#include "dkli.dkh"
#!
module "list"
{
    #include "functions.dkh"
    #include "serialize.dkh"
    //#include "../config.dk"
    #include "dbr.dkh"
    #include "dbr.apps.dkl"
    #include "uielements.v12.dkl"

    do @context.begin(@crud_context)

    ref data = @("&./")

    new table_definition
    {
        @"datatable*": data
        @"table_footer": "Mostrando los primeros # filas"
        member @"columns"
        {
            member @"codigo" { @"caption":"Código" }
            member @"descripcion" { @"caption":"Descripción" }
            member @"_actions" 
            { 
                @"caption":"Acciones" 
                @"template":ftext('
                    <div style="display:flex;gap:8px;justify-content:center;width:100%;" class="no-print">
                        <a href="#<@UBICACIONES>${DataRow.sys_pk}/" style="cursor:pointer; text-decoration: none; padding: 0px 8px; background-color: #F5F5F5; color: #000;">Editar</a>
                        <button onclick="InduxsoftCrudlModel.Delete(${DataRow.sys_pk})" style="cursor:pointer; border: none; outline: none; padding: 0px 8px; background-color: #F5F5F5; color: #000;">Eliminar</button>
                    </div>
                ')
                @"no-print":@true
            }
        }
    }

    header_act::
    { 
        #$ a(href="#<@UBICACIONES>_new/" class="btn btn-sm border-secondary induxsoft-corners btn-main-action no-print"){$"Agregar"}
    }

    #! point general_act to header_act

    new nav_config
    {
        @"title": "Ubicaciones"
        @"buttons*": general_act
        @"urlexit": "/"
        @"jscurrentcontext": "getCurrentContext"
    }
    
    #$
    div(class="container-fluid p-0")
    {
        #! do uie.printV12FormBar(nav_config)

        #$ div(id="work_area" class="container-section p-2")
        {
            form(action="./" method="GET" id="form_search" class="no-print my-1 d-flex py-1")
            {
                "filter-text"(
                    "id"="ft_search"
                    "text-field"="s"
                    "placeholder"="Buscar.."
                    "form"="#form_search"
                    "value"="#<@@(@@(@crud_context,'&http/request/get'),'$s')>"
                    "auto-submit"="true"
                    "class"="w-100"
                ){$""}
            }
            #! do uie.dbeditable(table_definition, "tbl_ubicaiones")
        }
        #$ script{$'
            function getCurrentContext()
            {
                const table = document.querySelector("#tbl_ubicaiones");
                const id = (table?.DataArray[table.CurrentRowIndex()]?.sys_pk ?? "");
                return { item_id:id, context: {} }
            }
        '}
    }
}